空气质量数据集为时间序列数据集，具体介绍如下：样本数：9358，特征数：15
特征说明：
0.日期（日/月/年）
1.次（HH.MM.SS）
2.实际小时平均浓度CO（mg/m^3）（参考分析仪）
3 .PT08.S1（氧化锡）每小时平均传感器响应（标称CO目标）
4.真实的每小时平均总非偏氢碳氢化合物浓度，单位为微克/立方米（参考分析仪）
5.真实的每小时平均苯浓度，单位为微克/立方米（参考分析仪）
6. PT08.S2（二氧化钛）每小时平均传感器响应（标称NMHC目标）
7.实际每小时平均氮氧化物浓度（ppb）（参考分析仪）
8. PT08.S3（氧化钨）每小时平均传感器响应（标称氮氧化物目标）
9.真实的小时平均NO2浓度，单位为微克/立方米（参考分析仪）
10 .PT08.S4（氧化钨）每小时平均传感器响应（标称NO2目标）
11. PT08.S5（氧化铟）每小时平均传感器响应（标称以O3为目标）
12.温度（°C）
13.相对湿度（%）
14. AH绝对湿度

神经元与激活函数
神经元：是神经网络的基本处理单元，类似于生物神经元。每个神经元接收多个输入信号，对这些输入进行加权求和，并通过一个激活函数来决定是否激活并输出信号。
激活函数：用于引入非线性因素，使神经网络能够处理更复杂的非线性问题。常见的激活函数有 Sigmoid 函数、ReLU 函数等。

网络结构
输入层：负责接收外部输入数据，将数据传递给下一层。输入层的神经元数量通常与输入数据的特征数量相等。
隐藏层：位于输入层和输出层之间，可以有多个隐藏层。隐藏层中的神经元通过权重与输入层和其他层的神经元相连，对输入信息进行处理和特征提取。随着隐藏层深度的增加，神经网络能够学习到更复杂的特征表示。
输出层：根据任务类型输出最终的结果。例如，在分类任务中，输出层的神经元数量通常与类别数量相等，每个神经元表示属于该类别的概率；在回归任务中，输出层通常只有一个神经元，输出一个连续的数值。

前向传播
在前向传播过程中，输入数据从输入层开始，依次经过各个隐藏层，最终到达输出层。在每一层中，神经元将接收到的输入信号进行加权求和，并通过激活函数进行处理，然后将结果传递到下一层。

反向传播
反向传播用于计算损失函数关于网络参数（权重和偏置）的梯度，并根据梯度来更新参数，以最小化损失函数。损失函数用于衡量模型预测结果与真实标签之间的差异，常见的损失函数有均方误差（MSE）、交叉熵损失等。
